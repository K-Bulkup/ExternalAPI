<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.external.auth.mapper.AuthMapper">

    <insert id="createUser" parameterType="com.external.auth.domain.UserVO">
        INSERT INTO users (user_id, bank, account_num, fintech_use_num)
        VALUES (#{userId}, #{bank}, #{accountNum}, #{fintechUseNum})
    </insert>

</mapper>
